 - MM application : 
          - multicast  / bordcast .. 
          - socket.setRecvBufferSize(size) : Sets the SO_RCVBUF socket option. Sets the maximum socket receive buffer in bytes.
          - socket.setSendBufferSize(size) : Sets the SO_SNDBUF socket option. Sets the maximum socket send buffer in bytes.
          - socket.setTTL(ttl) => ( Time to Live): The argument to socket.setTTL() is a number of hops between 1 and 255. The default on most systems is 64 but can vary.
  